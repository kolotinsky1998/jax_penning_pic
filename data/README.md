# Data Files

`cross_sections/` contains the collision cross-section tables copied from the
C++ reference project. The package also keeps a copy under
`jax_penning_pic/data/cross_sections/` so installed package runs can find the
default tables without depending on this top-level data mirror.
